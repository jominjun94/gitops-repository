apiVersion: v1
kind: Service
metadata:
  name: eks-project-service
  labels:
    app: eks-project
spec:
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: eks-project
  type: NodePort

      


